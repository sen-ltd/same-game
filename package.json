{
  "name": "same-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Classic Same Game puzzle — clear groups of same-color blocks for exponential score.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
